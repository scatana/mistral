{% extends 'base.njk' %}

{% set baseURL = "/" + getLocale() %}

{% block styles %}
  <link rel="stylesheet" href="{{'css/landing.css' | asset}}" />
{% endblock %}

{% block body %}
  {% include 'structured_data/services.njk' %}
  <div id="container">
    <header>
      <nav>
        <div class="brand">
          <a href="{{ baseURL }}">
            <img src="{{'img/logo_72-white.png' | asset}}" class="logo" alt="Logo" />
          </a>
          <div class="name">
            <span class="fname">{{ __('brand.contact.fname') }}</span>
            <span class="lname">{{ __('brand.contact.lname') }}</span>
          </div>
        </div>
        <div class="nav-menu">
          <a href="{{ baseURL }}/{{ __('routes.services') }}">{{ __('ui.nav-menu.services') }}</a>
          <a href="{{ baseURL }}/{{ __('routes.rates') }}">{{ __('ui.nav-menu.rates') }}</a>
          <a href="{{ baseURL }}/{{ __('routes.contact') }}">{{ __('ui.nav-menu.contact') }}</a>
          <a href="{{ baseURL }}/{{ __('routes.testimonials') }}">{{ __('ui.nav-menu.testimonials') }}</a>
        </div>
      </nav>
    </header>
    <main></main>
    <footer></footer>
  </div>
{% endblock %}
